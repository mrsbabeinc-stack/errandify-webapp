services:
  - type: web
    name: errandify-webapp
    env: node
    plan: free
    buildCommand: npm install && npm run build --prefix frontend
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
