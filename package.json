{
  "name": "errandify-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "dev": "cd frontend && npm run dev",
    "postinstall": "npm run build"
  }
}
