# Staging Environment Configuration

# Backend Configuration
PORT=3000
NODE_ENV=staging

# Database - PostgreSQL
DATABASE_URL=postgresql://errandify:staging-password@postgres:5432/errandify_staging

# JWT Secret (change this in production!)
JWT_SECRET=staging-jwt-secret-change-this-in-production

# Alibaba Qwen API (for AI features)
QWEN_API_KEY=sk-ws-H.IEXLEL.jcdb.MEQCIAOSCsPNf3qEu-g0kMlLS-TIxPA6MTI9je8IPyDDhO6jAiAY5ro3t8KEY0WGE5LyWThUNLEl7NQB6bIPofMzUqaufg

# Mapbox Geocoding API (for Singapore address lookup)
MAPBOX_API_KEY=your-mapbox-api-key-here

# Stripe (mock for staging)
STRIPE_SECRET_KEY=sk_test_staging_key
STRIPE_PUBLISHABLE_KEY=pk_test_staging_key

# SingPass Integration (set to false for staging)
USE_SINGPASS=false
SINGPASS_CLIENT_ID=staging-client-id
SINGPASS_CLIENT_SECRET=staging-client-secret

# Frontend
VITE_API_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Staging Database Password
DB_PASSWORD=staging-password
